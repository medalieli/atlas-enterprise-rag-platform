[project]
name = "production-rag-assistant-backend"
version = "0.1.0"
description = "FastAPI backend for the Production RAG Assistant"
requires-python = ">=3.12,<3.13"
dependencies = [
    "alembic>=1.16.5,<2",
    "asyncpg>=0.30.0,<0.31",
    "celery[redis]>=5.5.3,<5.6",
    "fastapi>=0.116.1,<0.117",
    "openai>=1.101.0,<2",
    "pgvector>=0.4.1,<0.5",
    "pydantic-settings>=2.10.1,<3",
    "python-multipart>=0.0.20,<0.1",
    "python-docx>=1.2.0,<2",
    "pypdf>=6.0.0,<7",
    "huggingface-hub==1.28.0",
    "safetensors==0.8.0",
    "sentence-transformers==6.0.0",
    "sqlalchemy[asyncio]>=2.0.43,<3",
    "tiktoken>=0.11.0,<0.12",
    "torch==2.13.0",
    "uvicorn[standard]>=0.35.0,<0.36",
]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[dependency-groups]
dev = [
    "httpx>=0.28.1,<0.29",
    "pytest>=8.4.1,<9",
    "pytest-asyncio>=1.1.0,<2",
    "ruff>=0.12.9,<0.13",
]

[tool.pytest.ini_options]
addopts = "-ra"
asyncio_mode = "auto"
asyncio_default_test_loop_scope = "session"
markers = ["integration: requires the Compose PostgreSQL service"]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
