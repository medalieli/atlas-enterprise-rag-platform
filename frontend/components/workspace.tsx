"use client";
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useRef, useState } from "react";
import { api, ApiError, key, logout as endSession, type Citation, type Collection, type Conversation, type DocumentItem, type Identity, type Message, type Turn, type Version } from "@/lib/api";

const terminal = new Set(["succeeded", "failed", "cancelled"]);
function errorText(error: unknown) { if (error instanceof ApiError) { if (error.status === 401) return "Your session expired. Sign in again."; if (error.status === 403) return "You do not have permission for this action."; if (error.status === 404) return "That item is no longer available."; if (error.status === 429) return "Too many requests. Wait a moment and retry."; if (error.status >= 500) return "The service is temporarily unavailable."; return error.message; } return "Something went wrong."; }
function Status({ value }: { value: string }) { return <span className={`badge ${value.replaceAll("_", "-")}`}><span aria-hidden="true" className="dot" />{value.replaceAll("_", " ")}</span>; }

export function Workspace({ initialView }: { initialView: "chat" | "documents" }) {
  const router = useRouter();
  const [identity, setIdentity] = useState<Identity>(); const [collections, setCollections] = useState<Collection[]>([]); const [collectionId, setCollectionId] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(true); const [mobile, setMobile] = useState(false); const [createOpen, setCreateOpen] = useState(false);
  const membership = identity?.memberships.find(x => collections.find(c => c.id === collectionId)?.tenant_id === x.tenant_id) ?? identity?.memberships[0];
  async function load() { try { const me = await api<Identity>("/auth/me"); setIdentity(me); const groups = (await Promise.all(me.memberships.map(m => api<Collection[]>(`/collections?tenant_id=${m.tenant_id}`)))).flat(); setCollections(groups); const saved = sessionStorage.getItem("collection"); setCollectionId(groups.some(c => c.id === saved) ? saved! : groups[0]?.id || ""); } catch (e) { setError(errorText(e)); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  useEffect(() => { if (collectionId) sessionStorage.setItem("collection", collectionId); }, [collectionId]);
  async function logout() { try { await endSession(); } finally { router.push("/"); router.refresh(); } }
  if (loading) return <main className="loading-shell" aria-busy="true"><div className="skeleton sidebar-skeleton"/><div className="skeleton content-skeleton"/><span className="sr-only">Loading workspace</span></main>;
  return <div className="app-shell">
    <header className="mobile-header"><button className="icon-button" aria-label="Open navigation" onClick={() => setMobile(true)}>☰</button><span className="brand">Atlas</span><span className="avatar">{membership?.role[0].toUpperCase()}</span></header>
    {mobile && <button className="scrim" aria-label="Close navigation" onClick={() => setMobile(false)} />}
    <aside className={`sidebar ${mobile ? "open" : ""}`} aria-label="Primary navigation"><div className="brand-row"><span className="brand-mark small">A</span><span><b>Atlas</b><small>Knowledge workspace</small></span><button className="close-nav" aria-label="Close navigation" onClick={() => setMobile(false)}>×</button></div>
      <label className="select-label" htmlFor="collection">Collection</label><select id="collection" value={collectionId} onChange={e => setCollectionId(e.target.value)}>{collections.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select>
      {!collections.length && <div className="sidebar-empty">No collections yet.</div>}
      <nav><Link className={initialView === "chat" ? "active" : ""} href="/chat">◫ <span>Chat</span></Link><Link className={initialView === "documents" ? "active" : ""} href="/documents">▤ <span>Documents</span></Link></nav>
      <div className="user-card"><span className="avatar">{membership?.role[0].toUpperCase()}</span><span><b>Current user</b><small>{membership?.role ?? "member"}</small></span><button aria-label="Log out" onClick={() => void logout()}>↪</button></div>
    </aside>
    <main className="workspace" id="main-content">{error && <div className="alert error" role="alert">{error}</div>}
      {!collections.length ? <EmptyCollections admin={identity?.memberships.some(m => m.role === "admin") ?? false} onCreate={() => setCreateOpen(true)} /> : initialView === "chat" ? <Chat collectionId={collectionId}/> : <Documents collectionId={collectionId} role={membership?.role ?? "viewer"}/>}
    </main>
    {createOpen && identity && (
      <CreateCollection tenant={identity.memberships.find(m => m.role === "admin")?.tenant_id ?? ""} onClose={() => setCreateOpen(false)} onCreated={c => { setCollections(x => [...x, c]); setCollectionId(c.id); setCreateOpen(false); }}/>
    )}
  </div>;
}
function EmptyCollections({ admin, onCreate }: { admin: boolean; onCreate(): void }) { return <section className="empty"><div className="empty-icon">◇</div><h1>No collections available</h1><p>{admin ? "Create a collection to organize trusted documents." : "Ask an administrator to grant access to a collection."}</p>{admin && <button className="button primary" onClick={onCreate}>Create collection</button>}</section>; }
function Dialog({ title, children, onClose }: { title: string; children: React.ReactNode; onClose(): void }) { const ref = useRef<HTMLDialogElement>(null); useEffect(() => { ref.current?.showModal(); ref.current?.querySelector<HTMLElement>("input,button")?.focus(); }, []); return <dialog ref={ref} onCancel={onClose}><div className="dialog-head"><h2>{title}</h2><button aria-label="Close dialog" onClick={onClose}>×</button></div>{children}</dialog>; }
function CreateCollection({ tenant, onClose, onCreated }: { tenant: string; onClose(): void; onCreated(c: Collection): void }) { const [name,setName]=useState(""); const [error,setError]=useState(""); return <Dialog title="Create collection" onClose={onClose}><form onSubmit={async e => { e.preventDefault(); try { const created=await api<Collection>("/collections", { method:"POST", body: JSON.stringify({ tenant_id:tenant, name }) }); onCreated(created); onClose(); } catch(x){setError(errorText(x));} }}><label>Name<input autoComplete="off" required maxLength={200} value={name} onChange={e=>setName(e.target.value)}/></label>{error&&<p className="field-error" role="alert">{error}</p>}<div className="dialog-actions"><button type="button" onClick={onClose}>Cancel</button><button className="button primary">Create</button></div></form></Dialog>; }

function Documents({ collectionId, role }: { collectionId: string; role: string }) {
  const [docs,setDocs]=useState<DocumentItem[]>([]); const [selected,setSelected]=useState<DocumentItem>(); const [upload,setUpload]=useState(false); const [error,setError]=useState("");
  const reload=async()=>{try{setDocs(await api<DocumentItem[]>(`/collections/${collectionId}/documents`));}catch(e){setError(errorText(e));}};
  useEffect(()=>{void reload();},[collectionId]);
  return <><header className="page-head"><div><p className="eyebrow">KNOWLEDGE BASE</p><h1>Documents</h1><p>Manage indexed sources and monitor their lifecycle.</p></div>{role!=="viewer"&&<button className="button primary" onClick={()=>setUpload(true)}>＋ Upload document</button>}</header>{error&&<div className="alert error" role="alert">{error}</div>}
    {!docs.length?<section className="empty panel"><div className="empty-icon">▤</div><h2>No documents yet</h2><p>Upload a PDF or DOCX to make its knowledge searchable.</p></section>:<section className="panel table-panel"><table><caption className="sr-only">Documents in this collection</caption><thead><tr><th>Document</th><th>Status</th><th>Active version</th><th>Updated</th><th><span className="sr-only">Actions</span></th></tr></thead><tbody>{docs.map(d=><tr key={d.id}><td><b>{d.filename||"Processing upload"}</b><small>{d.content_type?.includes("pdf")?"PDF":"DOCX"}</small></td><td><Status value={d.status}/></td><td>{d.active_version_number?`v${d.active_version_number}`:"Not active"}</td><td>{new Date(d.updated_at).toLocaleDateString()}</td><td><button className="link-button" onClick={()=>setSelected(d)}>View details</button></td></tr>)}</tbody></table></section>}
    {upload&&<Upload collectionId={collectionId} onClose={()=>setUpload(false)} onDone={()=>{setUpload(false);void reload();}}/>}{selected&&<DocumentDetails document={selected} role={role} onClose={()=>setSelected(undefined)} onChange={()=>void reload()}/>}</>;
}
function Upload({collectionId,onClose,onDone,replacement}:{collectionId:string;onClose():void;onDone():void;replacement?:string}) { const [file,setFile]=useState<File>(); const [metadata,setMetadata]=useState(""); const [status,setStatus]=useState(""); const [error,setError]=useState(""); const timer=useRef<ReturnType<typeof setTimeout> | undefined>(undefined); useEffect(()=>()=>clearTimeout(timer.current),[]);
  async function poll(job:string,n=0){if(n>=60){setError("Processing is taking longer than expected. Check again later.");return;}try{const result=await api<{status:string;error_message:string|null}>(`/processing-jobs/${job}`);setStatus(result.status);if(terminal.has(result.status)){if(result.status==="succeeded")onDone();else setError("Processing failed. The source was not activated; retry with a valid document.");}else timer.current=setTimeout(()=>void poll(job,n+1),2000);}catch(e){setError(errorText(e));}}
  return <Dialog title={replacement?"Upload replacement":"Upload document"} onClose={onClose}><form onSubmit={async e=>{e.preventDefault();if(!file)return;setStatus("uploading");const body=new FormData();body.set("file",file);if(metadata.trim()) { try{JSON.parse(metadata);body.set("metadata",metadata);}catch{setError("Metadata must be valid JSON.");return;} }try{const path=replacement?`/collections/${collectionId}/documents/${replacement}/versions`:`/collections/${collectionId}/documents`;const result=await api<{job_id:string}>(path,{method:"POST",body,headers:replacement?{"idempotency-key":key()}:undefined});setStatus("queued");void poll(result.job_id);}catch(x){setError(errorText(x));}}}><label>PDF or DOCX<input type="file" required accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={e=>setFile(e.target.files?.[0])}/></label><label>Metadata <span className="optional">optional JSON</span><textarea placeholder='{"department":"Operations","tags":["policy"]}' value={metadata} onChange={e=>setMetadata(e.target.value)}/></label>{status&&<div className="progress" aria-live="polite"><span className="spinner"/> {status.replaceAll("_"," ")}</div>}{error&&<p className="field-error" role="alert">{error}</p>}<div className="dialog-actions"><button type="button" onClick={onClose}>Cancel</button><button className="button primary" disabled={!!status&&!terminal.has(status)}>{replacement?"Replace":"Upload"}</button></div></form></Dialog>;
}
function DocumentDetails({document,role,onClose,onChange}:{document:DocumentItem;role:string;onClose():void;onChange():void}) { const [versions,setVersions]=useState<Version[]>([]); const [replace,setReplace]=useState(false); const [confirm,setConfirm]=useState(false); const [error,setError]=useState(""); useEffect(()=>{void api<Version[]>(`/collections/${document.collection_id}/documents/${document.id}/versions`).then(setVersions).catch(e=>setError(errorText(e)));},[document]); async function action(kind:"reindex"|"delete"){try{await api(`/collections/${document.collection_id}/documents/${document.id}${kind==="reindex"?"/reindex":""}`,{method:kind==="delete"?"DELETE":"POST",headers:kind==="reindex"?{"idempotency-key":key()}:undefined});onChange();onClose();}catch(e){setError(errorText(e));}}
  return <Dialog title={document.filename||"Document details"} onClose={onClose}><div className="detail-grid"><div><span>Status</span><Status value={document.status}/></div><div><span>Active version</span><b>{document.active_version_number?`Version ${document.active_version_number}`:"None"}</b></div><div><span>Active generation</span><code>{document.active_generation_id?.slice(0,8)??"None"}</code></div></div><h3>Version history</h3><ol className="versions">{versions.map(v=><li key={v.id}><div><b>Version {v.version_number}</b> · {v.filename}<small>{(v.size_bytes/1024).toFixed(1)} KB · {v.id}</small></div><Status value={v.active?"active":v.status}/>{v.failure_category&&<p>Processing failed safely. The prior active version remains available.</p>}</li>)}</ol>{error&&<p role="alert" className="field-error">{error}</p>}<div className="dialog-actions split">{role==="admin"&&<button className="danger" onClick={()=>setConfirm(true)}>Delete</button>}<span/>{role!=="viewer"&&<><button onClick={()=>setReplace(true)}>Replace</button><button className="button primary" onClick={()=>void action("reindex")}>Reindex</button></>}</div>{replace&&<Upload collectionId={document.collection_id} replacement={document.id} onClose={()=>setReplace(false)} onDone={()=>{setReplace(false);onChange();}}/>}{confirm&&<Dialog title="Delete document?" onClose={()=>setConfirm(false)}><p>This removes the source and indexed data. Historical citations become deleted-source notices.</p><div className="dialog-actions"><button onClick={()=>setConfirm(false)}>Cancel</button><button className="button danger-fill" onClick={()=>void action("delete")}>Delete document</button></div></Dialog>}</Dialog>;
}

function Chat({collectionId}:{collectionId:string}) { const [conversations,setConversations]=useState<Conversation[]>([]); const [current,setCurrent]=useState(""); const [messages,setMessages]=useState<Message[]>([]); const [query,setQuery]=useState(""); const [pending,setPending]=useState(false); const [error,setError]=useState(""); const [citation,setCitation]=useState<Citation>(); const answerRef=useRef<HTMLDivElement>(null);
  async function loadConversations(){try{const data=await api<{conversations:Conversation[]}>(`/collections/${collectionId}/conversations`);setConversations(data.conversations);const saved=sessionStorage.getItem(`conversation:${collectionId}`);const id=saved&&data.conversations.some(c=>c.id===saved)?saved:data.conversations[0]?.id||"";setCurrent(id);}catch(e){setError(errorText(e));}}
  useEffect(()=>{void loadConversations();},[collectionId]); useEffect(()=>{if(!current){setMessages([]);return;}sessionStorage.setItem(`conversation:${collectionId}`,current);void api<{messages:Message[]}>(`/collections/${collectionId}/conversations/${current}/messages?limit=100`).then(x=>setMessages(x.messages)).catch(e=>setError(errorText(e)));},[current,collectionId]);
  async function create(){try{const c=await api<Conversation>(`/collections/${collectionId}/conversations`,{method:"POST"});setConversations(x=>[c,...x]);setCurrent(c.id);}catch(e){setError(errorText(e));}}
  async function send(e:React.FormEvent){e.preventDefault();if(pending||!query.trim()||!current)return;const text=query.trim();setQuery("");setPending(true);setError("");setMessages(x=>[...x,{id:key(),sequence_number:999,role:"user",content:text,created_at:new Date().toISOString(),citations:[]}]);try{const result=await api<Turn>(`/collections/${collectionId}/conversations/${current}/messages`,{method:"POST",headers:{"idempotency-key":key()},body:JSON.stringify({query:text,top_k:8})});const content=result.clarification_question||result.answer?.answer||"No answer was returned.";const citations=result.answer?.citations||[];setMessages(x=>[...x,{id:key(),sequence_number:1000,role:"assistant",content,created_at:new Date().toISOString(),citations,answer_status:result.clarification_question?"clarification_required":result.answer?.status,rewriting_applied:result.rewriting_applied}]);requestAnimationFrame(()=>answerRef.current?.focus());}catch(e){setError(`${errorText(e)} Your question was not duplicated; you can retry.`);}finally{setPending(false);}}
  return <div className="chat-layout"><aside className="conversation-list"><div><h2>Conversations</h2><button aria-label="New conversation" onClick={()=>void create()}>＋</button></div>{conversations.map((c,i)=><button className={current===c.id?"selected":""} key={c.id} onClick={()=>setCurrent(c.id)}><b>{i===0?"Latest conversation":`Conversation ${conversations.length-i}`}</b><small>{new Date(c.updated_at??c.created_at).toLocaleDateString()}</small></button>)}{!conversations.length&&<p>No conversations yet.</p>}</aside><section className="chat-main"><header className="page-head compact"><div><p className="eyebrow">GROUNDED ASSISTANT</p><h1>Ask your knowledge base</h1></div><button className="button" onClick={()=>void create()}>＋ New chat</button></header><div className="messages" aria-live="polite">{!current?<div className="empty"><div className="empty-icon">✦</div><h2>Start a conversation</h2><p>Create a chat, then ask a question grounded in this collection.</p><button className="button primary" onClick={()=>void create()}>New conversation</button></div>:!messages.length?<div className="chat-welcome"><h2>What would you like to know?</h2><p>Answers use only authorized, active document versions.</p></div>:messages.map((m,i)=><article key={m.id} className={`message ${m.role} ${m.answer_status||""}`} ref={i===messages.length-1&&m.role==="assistant"?answerRef:undefined} tabIndex={i===messages.length-1&&m.role==="assistant"?-1:undefined}><span className="message-role">{m.role==="user"?"You":"Atlas"}</span>{m.answer_status&&m.answer_status!=="answered"&&<strong className="answer-state">{m.answer_status==="clarification_required"?"Clarification needed":m.answer_status==="insufficient_context"?"Not enough document context":"Sources conflict"}</strong>}{m.rewriting_applied&&<small className="rewrite-note">Follow-up interpreted using this conversation</small>}<p>{m.content}</p>{m.citations.length>0&&<div className="citation-row" aria-label="Citations">{m.citations.map(c=><button key={c.citation_number} onClick={()=>setCitation(c)} aria-label={`Open citation ${c.citation_number}`}>[{c.citation_number}] {c.document_name||"Deleted source"}</button>)}</div>}</article>)}{pending&&<div className="message assistant pending"><span className="spinner"/><span>Finding grounded evidence…</span></div>}{error&&<div className="alert error" role="alert">{error}</div>}</div><form className="composer" onSubmit={send}><label className="sr-only" htmlFor="question">Ask a question</label><textarea id="question" value={query} onChange={e=>setQuery(e.target.value)} placeholder={current?"Ask a question about your documents…":"Create a conversation first"} disabled={!current} maxLength={8000}/><button className="send" aria-label="Send question" disabled={pending||!query.trim()||!current}>↑</button><small>{pending?"This turn is processing. Other navigation remains available.":"Enter to add context · Answers include validated sources"}</small></form></section>{citation&&<CitationDrawer citation={citation} collectionId={collectionId} onClose={()=>setCitation(undefined)}/>}</div>;
}
function CitationDrawer({citation,collectionId,onClose}:{citation:Citation;collectionId:string;onClose():void}) { const ref=useRef<HTMLElement>(null);useEffect(()=>ref.current?.focus(),[]);const source=citation.document_id&&citation.document_version_id?`/api/backend/collections/${collectionId}/documents/${citation.document_id}/versions/${citation.document_version_id}/source${citation.page_number?`#page=${citation.page_number}`:""}`:null;return <aside className="drawer" aria-label={`Citation ${citation.citation_number}`} tabIndex={-1} ref={ref}><div className="drawer-head"><div><p className="eyebrow">SOURCE [{citation.citation_number}]</p><h2>{citation.document_name||"Deleted source"}</h2></div><button aria-label="Close citation" onClick={onClose}>×</button></div>{citation.source_status==="deleted"?<div className="tombstone"><b>This source was deleted</b><p>Its private excerpt and identifiers are no longer available.</p></div>:<><dl><div><dt>Version ID</dt><dd><code>{citation.document_version_id}</code></dd></div><div><dt>Location</dt><dd>{citation.page_number?`Page ${citation.page_number}`:citation.section_path||"Document section"}</dd></div><div><dt>Offsets</dt><dd>{citation.start_offset}–{citation.end_offset}</dd></div></dl><h3>Exact excerpt</h3><blockquote>{citation.source_excerpt}</blockquote>{Object.keys(citation.metadata||{}).length>0&&<><h3>Metadata</h3><dl>{Object.entries(citation.metadata).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{Array.isArray(v)?v.join(", "):String(v)}</dd></div>)}</dl></>}{source&&<a className="button primary wide" href={source} target="_blank" rel="noreferrer">Open source {citation.page_number?`at page ${citation.page_number}`:""}</a>}</>}</aside>; }
